## Рязань, парк. Узел области (DESIGN.md §9).
##
## Здесь игрок впервые упирается: змеиная дверь в конце не открывается,
## и единственный путь дальше — заметить спуск в подвал. Это первая петля
## метроидвании в игре, поэтому спуск нарочно на виду, а не спрятан.
extends Room

const YARD := "res://src/levels/ryazan/yard.tscn"
const CELLAR := "res://src/levels/ryazan/cellar.tscn"
const TRAIN := "res://src/levels/transsib/train.tscn"


func _define() -> void:
	room_id = "ryazan_park"
	room_title = "Парк"

	bounds(0, 0, 64, 30)

	# Пол с проёмом на x = 26..38 — спуск в подвал.
	block(0, 24, 26, 6)
	block(38, 24, 26, 6)
	block(0, 0, 64, 2)
	block(0, 2, 1, 22)
	block(63, 2, 1, 22)

	# Дно ямы на два тайла ниже пола: спрыгнуть легко, выпрыгнуть тоже.
	block(26, 26, 12, 4)

	# Лестница деревьев к осколку наверху.
	block(6, 20, 4, 1)
	block(12, 17, 4, 1)
	block(18, 14, 4, 1)

	# Ступени в правой части — просто платформинг по дороге к двери.
	block(44, 21, 5, 1)
	block(51, 19, 5, 1)

	# Массив над змеиной дверью: гейт нельзя перепрыгнуть.
	block(56, 2, 4, 16)

	spawn("start", 5, 24)
	spawn("west", 5, 24)
	# Возврат из подвала — в стороне от двери вниз, иначе игрок
	# провалился бы обратно тем же кадром.
	spawn("cellar", 35, 26)
	# Возврат из вагона — за змеиной дверью, а не перед ней.
	spawn("exit", 60, 24)

	shard(20, 14, "ryazan_03", "Парк")

	# Выход из области. Открывается Змееустом, который лежит в подвале.
	snake_door(58, 24, "ryazan_exit", 96.0)

	# За дверью — то, ради чего вся петля: выход из области.
	shard(61, 22, "ryazan_04", "За калиткой")
	dialogue(60, 24, "ryazan_transsib", "Пинки")

	door(2, 24, YARD, "east")
	door(28, 26, CELLAR, "start")
	door(62, 24, TRAIN, "ryazan")
